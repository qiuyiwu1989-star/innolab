# 2026-08-05

## 今天做了什么

- 上线**可分享的推演报告**：`/api/share` + `/r/<id>` 公开只读页，工作台"分享这份分析"按钮改成分享真实结果（原来只是把问题预填给对方）
- 补齐**个人中心**：资料可自己编辑、每条历史能一键生成分享链接、能改密码
- 把 InnoLab **对外 Skill 包做出来了**（v2.0.0）：
  - 新写对外版 `skill-dist/SKILL.md`（原来 720 行 → 200 行，砍掉 75% 只对自己有意义的内容）
  - 写了 `scripts/build-skill.py`：脱敏 + 自动同步方法数 + 敏感词闸门 + 完整性闸门
  - 出包 1.0MB，86 方法 / 76 案例 / 252 个文件，三道闸门全绿
- 建了这份**谱**（项目卡 + 进度 + 决定 + 要什么 + 接力包）

## 定了什么

- 对外 Skill 走**厚包 + 回流钩子**（不走薄包联网拉取）
- 对外版做成**构建产物**，私人版一个字不改
- **深脑和 InnoLab 不合并**，走记忆包协议接线；下行通道只开给邱懿武本人和顾问模式

## 试过但不行的

- **别用 `sed -i` 做中文批量替换**——macOS 上会挂，改用 Python 脚本
- **别在后台命令里加 `&`**——会把进程弄死
- **`deploy.py` 的输出是全缓冲的**——跑起来长时间没输出是正常现象，
  今天为此误判过一次"部署挂了"，其实只是还在传
- **闸门写完必须做负向测试**——今天故意注入敏感词验证过它真的会拦，没触发过的安全网等于没有

## 用户的原话（值得留的）

- 「key 这些不要写入 GitHub」
- 「我现在这是一个独立的网站，但是如果我想把它变成一个独立的 skill，可以被第三方 agent 使用」
- 「你和深脑的关系应该是怎么样的呢」
- 「个人管理需要完善下」

## 还没想清楚的

- 对外 Skill 的**第三方盲测还没跑完**（跑了一次中途断了），不知道真装到别人环境里会不会有坑
- 深脑的资料到底从哪个入口进来——这个不定，顾问现场仪表盘的"会前预载"就做不了
- 方法库的结构调整（合并/删除几个方法）一直悬着，因为会改 ID，不敢随手动

## 下次从哪开始

两条路挑一条：
1. **补完盲测**——找个不懂 InnoLab 的 agent 装上包跑一道真题，确认第三方真能用，然后发布
2. **顾问现场仪表盘**——这是邱懿武自己现场交付用的，价值密度最高，但要先回答深脑入口的问题
