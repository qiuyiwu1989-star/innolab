@AGENTS.md

# InnoLab · 项目卡

**一句话**：把模糊的商业问题，跑成一句能拍板的判断。线上跑着 → https://innolab.cc

## 这是给谁做的

主人是**邱懿武**（战略创新顾问、个人 IP）。这个产品是他的**咨询交付工具 + 获客前端**。

商业模式是**哑铃战略**，两头重中间轻：
- **获客端（免费）**：任何人来 innolab.cc 免费跑 AI 战略推演
- **转化端（收费）**：真正值钱的是邱懿武本人的 1:1 深度咨询

所以判断任何需求时的标准是：**它是在把人拉进来，还是在把人推向 1:1？** 两个都不是的功能，先放"以后再说"。

## 现在有什么

| 面 | 地址 | 是什么 |
|---|---|---|
| 网站 | innolab.cc | 给人用的工作台，免费试 + 留资 |
| MCP 端点 | innolab.cc/mcp | 给 agent 调的，私有 key 制 |
| Skill 包 | `dist/`（构建产物） | 给第三方 agent 装的离线方法体系 |

**核心资产**：`methods/` 里 86 个方法论（每个一张卡 + 一篇深度指南）+ `cases/` 里 76 个实战案例。
这三个面**共用同一份 `methods/` 和 `cases/`**——单一真源，改一处三处都变。

## 技术底子

Next.js 16（App Router）· React 19 · Tailwind 4 · TypeScript · Supabase（Auth + 数据）
推演引擎接小米 MiMo 大模型。一次完整推演约 4000–6000 字，耗时约 167 秒（模型本身慢，不是 bug）。

主要页面：`/demo` 工作台 · `/methods` 方法库 · `/cases` 案例 · `/me` 个人中心 · `/r/[id]` 分享报告 · `/mcp-guide` MCP 对接 · `/admin/*` 后台

## 怎么跑起来

```bash
npm run dev      # 本地开发
npx tsc --noEmit # 类型检查（改完必跑）
```

部署到线上服务器：见 `谱/进度.md` 里的部署那节（要密码，密码不在仓库里）。

## 绝对不能碰的

1. **密钥一个字都不许进 git。** 这个仓库是**公开**的。密钥只放服务器 `.env.local` 和密码管理器。
2. **不许编造。** 方法卡里不许出现编造的研究、百分比、实验结论——这是方法库的立身之本，之前清理过 5 处编造。
3. **不许动 `data/`。** 那是线上真实用户数据（推演记录、留资），部署脚本已把它排除在同步之外。
4. **改方法卡必须守 9 段结构**，原创方法不许瞎改（见 `references/METHOD_INDEX.md` 的 Origin 列）。
5. **对外分发的包必须走 `scripts/build-skill.py`**，它会脱敏真人姓名并做安全闸门；不许手工打包。

## 记事的规矩

- 拍板了 → 写进 `谱/决定.md`
- 做到哪了 → 更新 `谱/进度.md`
- 新功能动手前 → 先写 `谱/要什么/` 里的卡
- 每次收工 → 写 `谱/每天/YYYY-MM-DD.md`

安全相关的欠账记在 `谱/私密/` 里，**那个目录不进 git**。
