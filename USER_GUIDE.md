# InnoLab 使用说明书

> **一句话**：InnoLab 是邱懿武战略咨询的「授权专属推演工具」。
> 公开内容(方法库/案例库)谁都能看；真正的 AI 推演在「门内」，需暗号或 VIP 链接。
> 网址：**https://innolab.cc**

本说明书分三册，按身份看对应章节即可：
- 📖 **第一册 · 访客**（任何人，了解 InnoLab 是什么）
- 🔑 **第二册 · 授权用户**（合作伙伴 / 客户，用工作台推演）
- 🛠️ **第三册 · 管理员 & 运维**（邱懿武本人，看数据飞轮 + 部署运维）

---

## 📖 第一册 · 访客

### InnoLab 是什么
不是一个普通的 AI 问答。它装着邱懿武的 **83 个战略创新方法 + 76 个真实商业案例**，
对你的具体处境做一次专家级的结构化推演——问题重构 → 方法链 → 关键判断 → do/don't 结论 → 追问方向。
和 ChatGPT 的区别：同一个底层模型，被这套方法论体系「附身」了。

### 不登录能看什么（公开）
| 页面 | 网址 | 内容 |
|---|---|---|
| 首页 | https://innolab.cc | 是什么 / 怎么用 / 联系咨询 |
| 方法库 | /methods | 83 个方法，按 6 大引擎分类，可搜可筛 |
| 案例库 | /cases | 76 个真实商业推演案例 |
| 方法论体系 | /methodology | 6 大引擎 + 推演五步法 |
| 演示 | /demo | 5 个手写示范（不需暗号即可看输出长什么样） |
| 关于 | /about | 邱懿武 + 联系方式 |

### 想要真正用推演 / 找邱懿武
- 真正的实时推演工作台需要**授权暗号**（见第二册）。
- 没有暗号？首页或 /about 点「**和邱懿武聊聊**」联系获取。

---

## 🔑 第二册 · 授权用户（合作伙伴 / 客户）

### 你有两种进入方式

**方式 A · 通用暗号（合作伙伴）**
1. 打开 **https://innolab.cc/demo**
2. 输入授权暗号：**`innolab.cc`**
3. 填写：**称呼**（必填）+ **手机号或邮箱**（二选一，必填）+ 公司/行业（选填）
4. 点「进入工作台」→ 之后浏览器记住，不用反复输

**方式 B · VIP 专属链接（签约客户）**
- 直接打开你的专属链接，例如利丰集团：**https://innolab.cc/c/lifung-7h3q**
- 顶部显示「公司名 · 战略咨询专属」，**不限次**，无需输暗号

> 联系方式仅用于邱懿武本人回访，不会公开或他用。

### 怎么用工作台
1. **输入真实商业问题**（越具体越好，带上数字/背景）
   - ✅ 好例子：「我们 SaaS 年费 3000，产品比竞品强但续费率只有 61%，要不要涨到 12000？」
   - ❌ 差例子：「怎么做产品？」（太泛，推演不出有价值的判断）
2. **可选：填「你的背景」** → 推演会带上你的行业/阶段
3. 点「分析」（或 ⌘+Enter）→ 看引擎跑五步：读懂问题 → 检索方法库 → 匹配案例 → 编排推演链 → 生成判断
4. **输出**按固定结构：问题重构 / 调用方法 / 关键判断 / 推演结论(do/don't) / 追问方向
5. **多轮追问**：点底部追问按钮（继续追问 / 换用户角度 / 换竞品 / 换风险 / 换数据），引擎会**带着前文判断**继续想，不是从头开始

### 实用功能
- **复制结果 / 分享给朋友 / 收藏** —— 推演完成后的按钮
- **👍有用 / 👎不准** —— 点反馈帮邱懿武改进引擎（👎可写吐槽）
- **历史** —— 本浏览器保留你跑过的推演
- **不限次** —— 授权用户没有次数限制

### 用不了 / 报错
- 「暗号不正确」→ 确认暗号是否正确，或联系邱懿武
- 「需要授权」→ 你的浏览器没存授权，重新在 /demo 输一次暗号
- 推演中断 → 刷新重试；已生成的部分不会丢

---

## 🛠️ 第三册 · 管理员 & 运维（邱懿武本人）

### 一、看数据飞轮（最重要）

**飞轮指挥台**：**https://innolab.cc/admin/conversations?token=innolab-admin**

一屏看全四圈飞轮：
| 区块 | 看什么 | 用来 |
|---|---|---|
| **概览** | 总对话 / 领域数 / 授权方数 / 候选案例数 | 整体盘点 |
| **质量层** | 👍/👎 计数 + 所有👎(带用户吐槽) | **迭代引擎**：看哪些推演翻车 → 优化方法 |
| **画像层** | 每个留资用户：称呼/联系方式/提问几次/关注领域/提问史 | **懂画像 + 咨询线索** |
| **洞察层** | 领域热度 + 高频关键词 | **选选题**：该写哪个案例、卖哪类咨询 |
| **候选案例** | 标记过的好推演 | **沉淀内容**：将来转成正式案例 |
| **流水** | 最近 80 条对话，可展开看完整推演 | 逐条复盘 |

**操作**：每条对话有「标记为候选案例」按钮 → 把高价值推演存进候选队列。

**流量数据**（真人 PV/UV/来源/搜索词）→ 去**百度统计后台**看（已全站接入）。
**AI 爬虫**（GPTBot/ClaudeBot 抓取）→ 服务器 nginx 日志。

### 二、管理授权（发暗号 / 加 VIP 客户）

所有配置在服务器 `.env.local`，改完**必须重新部署**才生效。

```bash
# SSH 登录
ssh ubuntu@43.159.171.3        # 密码 Zaowu@806
nano /home/ubuntu/innolab/.env.local
```

```ini
# 通用暗号（发给合作伙伴，逗号分隔可多个）
INNOLAB_PASSCODES="innolab.cc,新暗号2"

# VIP 客户专属（token=公司名，分号分隔；链接为 /c/<token>）
INNOLAB_CLIENTS="lifung-7h3q=利丰集团;acme-x9k2=Acme科技"

# 后台令牌（看飞轮用）
ADMIN_TOKEN="innolab-admin"
```

- **加合作伙伴**：在 `INNOLAB_PASSCODES` 加一个暗号 → 重新部署 → 把暗号发给对方
- **加 VIP 客户**：在 `INNOLAB_CLIENTS` 加 `token=公司名` → 重新部署 → 把 `https://innolab.cc/c/<token>` 发给客户
- ⚠️ 改完 env **一定要重新部署**（见下），否则不生效

### 三、部署（更新代码 / 改配置后）

本机项目目录 `~/Documents/innolab-repo`，一条命令安全部署：

```bash
cd ~/Documents/innolab-repo
INNOLAB_SSH_PASS='Zaowu@806' python3 deploy.py
```

脚本会自动：① 本地 build 验证（失败则中止，不碰服务器）② 同步代码 ③ 服务器 build（失败则保留旧站点不挂）④ 重启 ⑤ 健康检查。**这是根除 502 的安全部署方式，不要手动 rm .next + 重启。**

### 四、git 同步（重要！）

线上靠 deploy.py 直接同步，**但 GitHub 仓库需要你手动推送**：
```bash
cd ~/Documents/innolab-repo
git push https://<你的GitHubToken>@github.com/qiuyiwu1989-star/innolab.git master
```
> ⚠️ 本地常领先 GitHub 很多 commit。线上是最新的，但 GitHub 滞后 = 万一服务器重拉会丢代码。**定期 push。**

### 五、加 / 改内容（方法 & 案例）

内容是**文件驱动**，改文件 = 网页和 AI 能力同时更新：
- **加方法**：在 `methods/<引擎>/` 下加 `.md`，Meta 里写 `- ID: XX##`
- **加案例**：在 `cases/<领域>/` 下加 `.json`，并在 `cases/case-index.json` 登记 + 改 `total_cases`
- 改完 → `deploy.py` 部署

### 六、关键信息速查

| 项 | 值 |
|---|---|
| 网址 | https://innolab.cc |
| 通用暗号 | `innolab.cc` |
| 利丰 VIP | https://innolab.cc/c/lifung-7h3q |
| 飞轮看板 | https://innolab.cc/admin/conversations?token=innolab-admin |
| 服务器 | ubuntu@43.159.171.3（pm2 进程 `innolab`，端口 3010） |
| 部署 | `INNOLAB_SSH_PASS='密码' python3 deploy.py` |
| AI 引擎 | 小米 MiMo `mimo-v2.5-pro`（OpenAI 兼容） |
| 数据落盘 | 服务器 `data/`：conversations / registrations / feedback / candidate-cases（不进 git） |

### 七、出问题怎么办
- **站点 502 / 打不开** → SSH 上去 `pm2 logs innolab` 看错误；多半是 build 没完成，重跑 `deploy.py`
- **暗号/VIP 不生效** → 确认改了 `.env.local` 且重新部署过
- **看不到飞轮数据** → 确认 URL 带 `?token=innolab-admin`
- **改了代码线上没变** → 是否跑了 `deploy.py`？build 是否 RC=0？

---

*最后更新：2026-05-30 · InnoLab by 邱懿武*
