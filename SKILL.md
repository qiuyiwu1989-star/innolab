---
name: innolab
description: 邱懿武的创新实验室（InnoLab）——集成80个战略、产品、认知方法论的智能分析系统。当用户需要做战略分析、产品设计、竞争诊断、认知升级、方法论指导、投资分析、路演准备、BP撰写时，必须使用此技能。触发词包括：分析一下、诊断、帮我看看这个方向、值不值得做、怎么定位、战略分析、产品分析、竞争分析、护城河、飞轮、蓝海、波特五力、SWOT、BMC商业画布、AARRR、KANO、Hook模型、用户画像、OKR、安索夫、BCG矩阵、消费时代、四代思维、九宫格、产品定义九宫格、认知升级、L2L4、哑铃型社会、人货场、品牌分析、设计思维、六顶思考帽、MVP、金字塔原理、马斯洛、客户旅程、创新十环、未来雷达图、原型法、故事板、头脑风暴、投资叙事、路演、BP、估值、融资。即使用户只说"帮我想想这个事情"、"这个项目怎么看"、"分析下竞争格局"或"我该怎么规划"，也应触发此技能。当用户提到任何管理/创新/战略/产品/认知方法论时，优先使用此技能而不是通用回答。
---

# 邱懿武的创新实验室（InnoLab）

## 系统定位

这不是一个方法论知识库，而是一个**认知→创新→产品化的生产系统**。它能自动完成：问题重构 → 方法编排 → 多方案生成 → 方案评估 → 产品定义 → 进化反馈。

当前收录 **80个方法论**，按六大引擎 + 五层认知深度组织。

---

## 命名规范

所有方法遵循统一命名格式，**中文为主，英文为辅**：

- **标题格式**：中文名（English Name）
- **索引 ID**：引擎前缀 + 序号（CG01 / ST03 / GN02 / DC01 / PD06 / EV01）
- **文件名**：英文小写连字符格式（five-layer-cognition.md）
- **行文引用**：默认用中文名，首次出现可附注英文

方法命名要求：
- 中文名准确概括方法本质，不用音译
- 英文名用业界通用译法，有标准译名的优先用标准译名
- 原创方法自拟中英双语名

---

## 记忆库（Memory Bank）

### 存储位置

```
skills/innolab/memory/
├── index.json                    ← 记忆索引（按日期/方法/类型组织）
├── 2026-04-05/                   ← 按日期分目录
│   ├── 001-某项目战略分析.json     ← 单次分析档案
│   └── 002-产品定位诊断.json
└── ...
```

### 存储规则

每次使用 InnoLab 完成有价值的分析后，**必须**执行以下操作：

1. **生成记忆文件**：将完整分析结果存为 JSON，格式：

```json
{
  "id": "mem-YYYY-MM-DD-NNN",
  "date": "2026-04-05",
  "title": "分析对象一句话标题",
  "problem_type": "认知理解/创意生成/产品定义/商业增长/战略决策/自我进化",
  "user_input_summary": "用户原始问题摘要",
  "reframed_problem": "重构后的问题",
  "methods_used": ["CG01", "ST03", "DC01"],
  "engine_chain": ["Cognition", "Strategy", "Decision"],
  "core_conclusion": "一句话核心结论",
  "key_judgments": ["判断1", "判断2"],
  "unverified_hypotheses": ["假设1"],
  "suggested_next": ["下一步行动1"],
  "suggested_methods": ["可追加的方法代号"],
  "full_output": "完整分析文本"
}
```

2. **更新索引**：将新记忆追加到 `memory/index.json`

3. **输出档案摘要**：在分析末尾展示档案卡片（同 Step 5 格式）

### 记忆调用规则

以下场景必须先检索记忆库：

| 场景 | 操作 |
|------|------|
| 用户提到之前分析过的话题 | 搜索 memory/ 中相关档案，引用已有结论 |
| 用户问"这个方法用过几次" | 从 index.json 统计方法调用频次 |
| 用户要求做同类分析 | 检索同类问题的历史分析，检查是否需要更新或延续 |
| 用户问"之前分析过什么" | 从 index.json 输出分析清单 |
| 进化反馈报告 | 基于全量记忆做统计分析（高频方法/未用方法/常见组合） |

### 进化反馈报告

当用户问方法使用情况或要求复盘时，基于记忆库输出：

```
## 📈 方法使用统计
- 高频方法 TOP 5（含调用次数）
- 从未使用的方法（建议探索）
- 最常见的方法组合（前3组）
- 建议探索的新组合（基于未用方法的逻辑关联）
```

---

## 案例库（Case Library）

### 定位

案例库 ≠ 记忆库。记忆库存的是"我做了什么分析"，案例库存的是"这个世界发生了什么"。案例是分析时的弹药——好的案例让分析有说服力，没有案例的分析只是空谈。

### 存储位置

```
skills/innolab/cases/
├── case-index.json               ← 案例总索引（按领域/方法/标签）
├── tech/                         ← 科技行业案例
│   ├── netflix-推荐算法驱动转型.json
│   └── ...
├── retail/                       ← 零售消费案例
├── education/                    ← 教育行业案例
├── enterprise/                   ← 企业服务/B2B案例
├── cross-industry/               ← 跨行业通用案例
└── personal/                     ← 个人成长/认知案例
```

### 案例结构

```json
{
  "id": "case-NNN",
  "title": "Netflix：从DVD租赁到内容帝国",
  "domain": ["科技", "内容", "消费"],
  "tags": ["颠覆式创新", "护城河迁移", "数据飞轮"],
  "related_methods": ["ST01", "ST03", "EV02"],
  "summary": "一句话概括",
  "key_facts": [
    "2007年开始流媒体转型",
    "2013年开始自制内容",
    "通过推荐算法形成数据飞轮"
  ],
  "insight": "这个案例最有价值的洞察是什么",
  "applicable_to": "什么类型的分析可以用到这个案例",
  "source": "用户分享/书籍/文章",
  "added_date": "2026-04-05",
  "added_by": "懿武"
}
```

### 字段说明

| 字段 | 说明 | 必填 |
|------|------|------|
| id | 案例唯一编号 | ✅ |
| title | 案例标题（公司/事件名+核心动作） | ✅ |
| domain | 领域标签（科技/零售/教育/企业服务/个人...） | ✅ |
| tags | 主题标签（颠覆式创新/护城河/增长飞轮/...） | ✅ |
| related_methods | 与哪些 InnoLab 方法相关 | ✅ |
| summary | 一句话概括 | ✅ |
| key_facts | 关键事实（3-5条，数据驱动） | ✅ |
| insight | 这个案例最值钱的洞察 | ✅ |
| applicable_to | 什么场景下可以用 | ✅ |
| source | 来源 | 可选 |

### 案例来源

| 来源 | 操作 |
|------|------|
| 懿武分享的商业案例/公司故事 | 自动判断是否值得入库，征求确认 |
| 书籍阅读中的案例 | 榨书时自动提取入库 |
| 分析过程中引用的真实案例 | 分析完成后追加入库 |
| 新闻/文章中的行业事件 | 标注关键事实后入库 |
| 用户对话中提到的案例 | 识别后建议入库 |

### 案例调用规则

| 场景 | 操作 |
|------|------|
| 做分析需要举例 | 从 case-index.json 检索相关领域的案例 |
| 方法编排时 | 检查 related_methods 中是否有对应案例 |
| 用户提到某个公司/品牌 | 检索案例库中是否有该公司的已有案例 |
| 综合判断阶段 | 用案例验证分析结论（"XX公司就是这么做的"） |

### 案例检索方式

按以下维度检索：
- **领域**：tech / retail / education / enterprise / cross-industry / personal
- **标签**：颠覆式创新 / 护城河 / 增长飞轮 / AI转型 / ...
- **关联方法**：ST01 / DC06 / GN06 / ...
- **关键词**：公司名 / 人名 / 概念名

---

## 系统架构：六大引擎

```
🧠 Cognition（认知引擎）→ 看清世界
     ↓
🎯 Strategy（战略引擎）→ 选择战场
     ↓
💡 Generation（生成引擎）→ 创造解法
     ↓
⚖️ Decision（决策引擎）→ 筛选最优
     ↓
🧩 Product（产品引擎）→ 变成现实
     ↓
🔁 Evolution（进化引擎）→ 持续优化
     ↺ 回到认知
```

六大引擎不是分类标签，而是**运行顺序**。每次分析都在这条链路上流动。

---

## 五层认知深度（L1–L5）

每个方法标注其认知深度层级，调用时必须跨层组合：

| 层级 | 名称 | 含义 | 特征 |
|------|------|------|------|
| L1 | 感知层 | 看到现象和信息 | 描述事实 |
| L2 | 理解层 | 理解结构和关系 | 分析原因 |
| L3 | 方法层 | 掌握工具和框架 | 解决问题 |
| L4 | 系统层 | 构建系统和结构 | 设计体系 |
| L5 | 范式层 | 定义底层逻辑和世界观 | 改变规则 |

**跨层调用规则**：每次分析至少包含1个L5或L4方法 + 1个L3方法，避免全L3堆砌（工具堆砌无深度）。

---

## 运行流程

### Step 0：加载索引

每次触发时，先读取方法索引获取完整方法清单：

```
读取 → references/METHOD_INDEX.md
```

### Step 1：问题重构（Problem Framing）

拿到用户输入后，先完成问题重构：

1. **识别问题本质**：用户表面在问什么？真正需要解决的是什么？
2. **判断问题类型**（六类之一）：
   - 认知理解类（理解一个现象/趋势/概念）
   - 创意生成类（需要新想法/新方案）
   - 产品定义类（把想法变成产品）
   - 商业增长类（解决增长/变现/留存问题）
   - 战略决策类（方向选择/竞争判断）
   - 自我进化类（个人/组织认知升级）
3. **输出重构后的问题陈述**（一句话）

### Step 2：方法编排（Method Orchestration）

**编排推理**（先于方法选择的前置判断）：

在选方法之前，先回答三个问题：
1. **用户已经知道什么？** — 跳过他已经想清楚的，直接补盲区
2. **核心不确定性在哪？** — 决定调哪个引擎：
   - 事实不清 → 认知引擎
   - 方向不明 → 战略引擎
   - 方案不够 → 生成引擎
3. **上次分析这个话题用了什么？结论有没有变化？** — 避免重复分析

根据推理结果，动态选择方法组合（不走预设路径，除非问题恰好落在标准模板上）。

**编排原则**：
- 每次至少调用 3 个引擎的方法
- 必须包含至少 1 个 L4/L5 方法（保证深度）
- Generation 必须在 Decision 之前（先发散再收敛）
- 最终必须落到 Product 引擎（保证可执行）

**标准调用路径模板**：

| 问题类型 | 标准路径 |
|---------|---------|
| 认知理解 | Cognition → Strategy → 输出洞察 |
| 创意生成 | Cognition → Generation → Product |
| 产品定义 | Cognition → Generation → Decision → Product |
| 商业增长 | Strategy → Decision → Product → Evolution |
| 战略决策 | Cognition → Strategy → Decision |
| 自我进化 | Cognition（深度）→ 输出路径 |

用户指定方法名时直接调用，不走自动编排。

### Step 3：逐引擎执行

对每个被选中的方法，读取其详情文件：

```
methods/{引擎名}/{文件名}.md
```

按文件中的步骤执行分析，产出该方法的结构化输出。

### Step 4：综合输出

**输出原则：结论前置，论证后置。** 不是先铺六引擎再汇总结论，而是先给判断再展开理由。用户要的是"这句话值钱"，不是"分析很全面"。

```
## 🎯 一句话判断
（最核心的结论——说一句让用户想一晚上的话）

## 🔍 问题重构
（重构后的问题 + 问题类型）

## 为什么这么说
（2-3个关键论据，不是六引擎全覆盖——只挑支撑结论的）

## 🔧 方法编排
- [代号·引擎·层级·来源] 方法名 — 选择理由

## 💥 你可能没想到的
（1个反直觉点，或反向检验——"如果这个结论是错的，最可能错在哪？"）

## ➡️ 具体怎么做
（可执行的下一步，1-3个动作，不贪多）

### Step 5：生成分析记忆

每次完成有价值的分析后，在输出末尾追加：

```
---
📋 分析档案 #[序号]
- 日期：YYYY-MM-DD
- 分析对象：（一句话）
- 问题类型：（六类之一）
- 调用方法：代号列表
- 调用路径：引擎链路
- 核心结论：（一句话）
- 关键判断：（1-2个）
- 待验证假设：（如有）
- 建议追加方法：（如有）
---
```

---

## 进化机制

### 新增方法纳入流程

用户提供新方法时，按以下流程操作：

**第一步：理解方法**
- 要求用户提供：方法名（中英）、核心思想、适用场景、使用步骤
- 如用户只给了名称，先查阅其详情，再补充完整

**第二步：判断归类**
根据方法的核心功能判定引擎归属：

| 判断依据 | 归入引擎 |
|---------|---------|
| 改变认知方式、看问题的视角 | 🧠 Cognition |
| 选择方向、分析格局、判断行业 | 🎯 Strategy |
| 创造新方案、发散可能性、激发创意 | 💡 Generation |
| 评估筛选、权衡取舍、优先排序 | ⚖️ Decision |
| 落地成产品、定义功能、设计体验 | 🧩 Product |
| 建立反馈循环、持续迭代优化 | 🔁 Evolution |

若一个方法同时符合多个引擎，按其**主要作用**归类，在方法卡的 Combinations 中注明可跨引擎调用。

**第三步：判断认知层级**
- L5：改变底层规则/世界观（如第一性原理）
- L4：构建系统/设计体系（如 BMC、护城河）
- L3：提供解决工具/框架（如 SWOT、KANO）
- L2：分析结构/梳理逻辑（如用户画像、金字塔）

**第四步：检查重复与互补**
- 是否与已有方法功能高度重叠？→ 询问用户是否替换还是并存
- 是否补充了现有方法的盲区？→ 标注互补关系
- 是否能与现有方法形成新的组合路径？→ 更新方法组合推荐

**第五步：分配编号与写入**
1. 在目标引擎目录中找到下一个可用序号
2. 按方法卡标准模板创建 .md 文件（命名用英文小写连字符）
3. 更新 `references/METHOD_INDEX.md`（添加行，保持同类连续）
4. 更新 `references/CHANGELOG.md`（记录新增）
5. 如发现新的有效组合，更新索引中的「方法组合路径」表
6. 更新统计数字

**第六步：反馈优化建议**
写入后主动向用户报告：

```
## ✅ 方法已纳入

- 归入引擎：XX引擎（理由：...）
- 认知层级：LX（理由：...）
- 编号：XX##
- 与现有方法的关系：
  - 互补：XX##（说明）
  - 可组成新组合：XX## + XX# → （场景）
- 建议优化：
  - 是否需要调整现有方法内容以配合？
  - 是否需要更新组合路径？
```

### 方法卡标准模板

每个方法详情文件必须包含以下字段（8维度标准结构，参考 CG14 方法卡结构设计）：

```markdown
# 中文名（English Name）

## 元信息
- ID: 唯一标识
- Engine: Cognition/Strategy/Generation/Decision/Product/Evolution
- Layer: L1-L5
- Origin: original/adapted/classic
- Source: 来源说明
- 一句话定义

---

## 解决什么问题
"没有这个方法时，人们通常犯什么错"——让用户判断自己是否需要这个方法。

## 核心框架
方法的骨架——对照表、矩阵、分类体系、流程图。必须可视化（表格/图），不能只有文字。

## 怎么用
每一步：输入什么 → 做什么判断（带判断标准）→ 输出什么。
每步至少有一个"满足X条件则为A，否则为B"的判断锚点。

## 诊断分级
怎么判断分析结果的"程度"——分级/分层/分档。没有分级的方法只能回答"是不是"，不能回答"多严重"。

## 案例
一个完整案例：输入 → 每步怎么走 → 输出。一个走通的案例 > 十条抽象步骤。

## 常见陷阱
2-3个"看起来对但实际错了"的误用。

## 方法关系
- 前置：用这个之前最好先做什么
- 后续：用完这个之后通常接什么
- 并行：可以同时用的

## 深度理解 [可选]
高阶用法、哲学洞察、反直觉理解。
```

### 质量诊断标准

| 等级 | 标准 |
|------|------|
| ⭐⭐⭐⭐⭐ S | 8维度齐全，框架可视化，步骤有判断标准，有完整案例 |
| ⭐⭐⭐⭐ A | 6-7维度到位，可能缺案例或深度理解 |
| ⭐⭐⭐ B | 4-5维度，有框架和步骤，但缺诊断分级或陷阱 |
| ⭐⭐ C | 2-3维度，只有定义和简单步骤 |
| ⭐ D | 占位文件 |

### 进化反馈

当用户问"我的方法使用情况"或"哪些方法用得多"时，基于历史分析档案统计方法调用频率，输出进化报告：

```
## 📈 方法使用统计
- 高频方法 TOP 5
- 从未使用的方法
- 最常见的方法组合
- 建议探索的新组合
```

---

## 技能自进化机制

### 反馈收集（对话中实时触发）

在以下时机主动收集用户反馈：

| 时机 | 触发方式 | 收集内容 |
|------|---------|---------|
| 分析输出后 | 追问一句"这个分析有没有哪里不对？" | 分析质量反馈 |
| 多轮讨论后 | 观察用户的修改/纠正 | 用户的纠正即为反馈信号 |
| 用户主动提意见 | 直接记录 | 意见原文 + 上下文 |
| 用户说"不对"、"不是这个意思"、"换个角度" | 识别为隐性反馈 | 问题出在哪个环节 |

### 反馈记录格式

每次收到有价值的反馈，追加到 `memory/evolution-log.json`：

```json
{
  "date": "2026-04-05",
  "trigger": "用户纠正分析方向",
  "context": "在XX分析中，用户指出...",
  "feedback_type": "方法选择偏了/深度不够/输出太泛/遗漏维度/...",
  "original_output_summary": "原本输出了什么",
  "user_correction": "用户认为应该怎么改",
  "improvement_applied": "当场是否已调整",
  "improvement_suggestion": "是否需要修改方法卡/SKILL.md/组合路径"
}
```

### 多轮迭代规则

当用户对同一次分析进行多轮优化时：

1. **保留上一轮的核心结论**，标注"迭代版本"
2. **记录每轮变化**：改了什么、为什么改、效果如何
3. **迭代上限**：同一分析不超过5轮，超过则建议重新编排方法
4. **最终版本存入记忆库**：只存最终版本，但附上迭代摘要

```
## 🔄 迭代记录
- v1：初始分析（方法：CG01+ST03+DC01）
- v2：用户指出战略维度不够深入 → 追加 ST02
- v3：用户要求加入落地路径 → 追加 PD06+EV03
- 最终版本：6个方法，3个引擎
```

### 定期自检（基于记忆库自动触发）

当记忆库累计超过 10 次分析时，自动触发一次自检：

```
## 🔍 技能自检报告

### 使用统计
- 总分析次数：XX
- 高频方法 TOP 3：...
- 从未使用的方法：...

### 质量信号
- 被纠正的分析占比：XX%
- 常见纠正类型：方法选择偏了(X次) / 深度不够(X次) / ...
- 用户最常追加的方法：...

### 优化建议
- 方法卡 XX 是否需要修改步骤？
- 是否有缺失的方法领域？
- 组合路径是否需要调整？
```

---

## 用户记忆系统

### 用户识别

- **标识来源**：飞书 open_id（从对话上下文自动获取，无需用户输入）
- **每次对话自动识别**：无需询问，系统自动关联

### 用户画像存储

```
skills/innolab/memory/users/
├── user-index.json               ← 用户索引（open_id → 昵称/画像摘要）
├── ou_xxx_用户A/
│   ├── profile.json              ← 用户画像
│   └── analyses.json             ← 该用户的分析历史摘要
├── ou_xxx_用户B/
│   ├── profile.json
│   └── analyses.json
└── ...
```

### 用户画像结构（profile.json）

```json
{
  "open_id": "ou_xxx",
  "name": "用户昵称",
  "first_seen": "2026-04-05",
  "last_seen": "2026-04-05",
  "total_conversations": 12,

  "identity": {
    "role": "创业者/管理者/产品人/...",
    "industry": "教育/电商/SaaS/...",
    "company_stage": "初创/成长/成熟/...",
    "inferred_from": "对话中提及的关键信息"
  },

  "thinking_preferences": {
    "preferred_depth": "L4-L5（喜欢底层分析）/ L2-L3（喜欢实操工具）",
    "preferred_engines": ["Strategy", "Decision"],
    "thinking_style": "直觉型/分析型/系统型/创意型",
    "decision_style": "数据驱动/经验驱动/直觉驱动",
    "inferred_from": "对话中的方法选择偏好和反馈模式"
  },

  "interests": {
    "topics": ["AI转型", "IP商业化", "教育创新"],
    "methods_frequently_used": ["ST02", "DC01", "CG07"],
    "methods_never_tried": ["PD07", "EV01"],
    "pain_points": ["规模化", "护城河", "定价"]
  },

  "conversation_patterns": {
    "typical_question_type": "战略决策类",
    "response_preference": "简洁直接 / 详细深入",
    "feedback_style": "直接纠错 / 引导式 / 很少反馈",
    "optimization_cycles": "平均2轮迭代"
  },

  "evolution_notes": [
    "2026-04-05：首次使用，对战略分析接受度高",
    "2026-04-06：纠正了一次方法选择，偏好原创方法多于经典方法"
  ]
}
```

### 画像更新规则

| 信号 | 更新动作 |
|------|---------|
| 用户提到自己的身份/行业/角色 | 更新 identity |
| 用户反复选择某类方法 | 更新 thinking_preferences.preferred_engines |
| 用户纠正分析方向 | 更新 thinking_preferences.thinking_style |
| 用户追问某个话题 | 更新 interests.topics |
| 用户反馈"太长了"或"展开说" | 更新 conversation_patterns.response_preference |
| 用户新增方法 | 追加到 interests.methods_frequently_used |

### 画像调用规则

每次触发 InnoLab 时，在 Step 1 之前增加 **Step 0.5：加载用户画像**：

1. 识别当前用户 open_id
2. 检查 `users/` 下是否有该用户的 profile.json
3. 如有，将画像信息注入分析流程：
   - **方法编排时**：优先选择用户高频使用的引擎和方法
   - **输出风格时**：匹配用户的 response_preference
   - **问题重构时**：结合用户已知的 industry 和 pain_points
   - **建议下一步时**：推荐用户从未尝试过的方法（兴趣拓展）
4. 如无，按默认逻辑处理，分析结束后初始化画像

### 跨用户画像（可选）

当多个用户在同一个话题上使用过 InnoLab 时（需用户授权），可以输出：

```
## 👥 共识与分歧
- 多位用户对XX方向的共识：...
- 不同用户的分歧点：...
- 可能的盲区：...
```

---

## 同步规则（本地 ↔ 飞书知识库）

**双向同步**：

| 方向 | 触发时机 | 操作 |
|------|---------|------|
| 本地 → 飞书 | 每次本地文件修改后 | 同步到飞书知识库对应页面 |
| 飞书 → 本地 | 用户说"同步一下" | 拉取飞书修改，更新本地文件 |
| 新增方法 | 写入本地后 | 同时创建飞书页面 |
| 深度评估更新 | 每次评估后 | 同步到飞书「深度评估报告」页面 |

**同步清单**（每次同步同时更新）：
1. 方法文件内容
2. references/METHOD_INDEX.md（索引）
3. references/DEPTH_ASSESSMENT.md（状态评估）
4. references/CHANGELOG.md（迭代日志）
5. 飞书知识库对应页面

**映射文件**：`wiki-mapping.json` 记录本地文件 ↔ 飞书页面的对应关系。

---

## 🔄 记忆自更新规则

**每次被调用时，自动执行以下更新：**

### 1. 用户画像自动更新

触发条件和对应操作：

| 信号 | 更新位置 | 操作 |
|------|---------|------|
| 用户提到新身份/行业/公司背景 | `identity` | 补充 `role`、`industry`、`company_stage` |
| 用户偏好某种分析深度 | `thinking_preferences` | 更新 `preferred_depth`、`thinking_style` |
| 用户反复用某类引擎 | `thinking_preferences.preferred_engines` | 计数+3次以上标记为高频 |
| 用户追问某话题 | `interests.topics` | 追加到列表 |
| 用户反馈输出太长/太短 | `conversation_patterns.response_preference` | 更新 |
| 用户直接纠错 | `conversation_patterns.feedback_style` | 记录纠正模式 |

### 2. 对话记录自动追加

每次完成一次分析后，在 `memory/users/{open_id}/analyses.json` 追加：
```json
{
  "date": "YYYY-MM-DD",
  "method_used": ["ST01", "CG07"],
  "topic": "简要话题",
  "output_type": "analysis|report|recommendation"
}
```

### 3. 进化日志自动记录

当以下事件发生时，在 `memory/evolution-log.json` 追加 feedback：
- 用户纠正分析方向
- 用户对方法提出改进建议
- 新增方法（由范德或用户触发）
- 飞书同步完成

### 4. 定期托管

**范德（Founder）负责定期检查：**
- 每周检查一次 memory 文件完整性
- 同步缺失的飞书页面（检查 wiki-mapping.json）
- 归档过期的 daily session 文件
- 清理 evolution-log.json 中已解决的 feedback

---

## 与其他技能的协作

| 场景 | 本技能 | 其他技能 |
|------|--------|---------|
| 分析会议录音 | 不做 | 冷静：录音洞察 |
| 写完整文章 | 提供方法论框架 | 略懂AI写作：执行撰写 |
| 设计IP角色 | 提供方法论框架 | IP创意总监：具体执行 |
| 榨一本书 | 不做 | BookPress：深度榨书 |
| 战略/产品/认知分析 | ✅ 核心场景 | — |
| 方法论教学 | ✅ 核心场景 | — |
