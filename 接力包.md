# InnoLab 接力包

> **给下一个 AI 的开场白**：
> 你接手的是一个**已经上线、有真实用户**的产品。项目主人叫**邱懿武**，是战略创新顾问，
> 他懂产品和业务，能读懂代码但不靠手写代码。请说人话，别掉书袋。
> 动手前先读项目里的 `CLAUDE.md` 和 `谱/` 目录——那里有全部来龙去脉。
> **最重要的一条：这个 git 仓库是公开的，任何密钥都不许写进去。**

生成于 2026-08-05

---

## 一、这是什么

**InnoLab** —— 把模糊的商业问题，跑成一句能拍板的判断。线上地址 https://innolab.cc

商业模式是**哑铃战略**：左边免费 AI 推演获客，右边邱懿武 1:1 咨询变现，中间不做重。
判断任何新需求，就问一句：**它在把人拉进来，还是在把人推向 1:1？** 都不是就先别做。

**核心资产**：86 个方法论（每个一张方法卡 + 一篇深度指南）+ 76 个实战案例。
这是邱懿武多年沉淀的东西，是产品的根。

**三个面共用同一份资产**：
| 面 | 地址 | 给谁 |
|---|---|---|
| 网站 | innolab.cc | 给人用 |
| MCP 端点 | innolab.cc/mcp | 给 agent 调（私有 key） |
| Skill 包 | `scripts/build-skill.py` 出包 | 给第三方 agent 装 |

---

## 二、技术底子

Next.js 16（App Router）· React 19 · Tailwind 4 · TypeScript · Supabase（登录 + 数据）
推演引擎接小米 MiMo 大模型。

**一个必须知道的事实**：一次完整推演要 **167 秒**（约 4000–6000 字）。
这不是 bug，是模型本身慢。所有超时设置、流式方案都是围绕这个数字设计的——
nginx 超时调到了 300 秒，MCP 走 SSE 每 10 秒发心跳保活。**别把它当性能问题去"优化"。**

```bash
npm run dev        # 本地开发
npx tsc --noEmit   # 类型检查，改完必跑
python3 scripts/build-skill.py   # 出对外 Skill 包
```

部署：`INNOLAB_SSH_PASS='<密码见密码管理器>' SKIP_LOCAL_BUILD=1 python3 deploy.py`
（脚本输出是全缓冲的，**跑起来很久没动静是正常的**，别以为挂了。）

---

## 三、现在能用的功能

- **免费推演工作台** `/demo`——三档配额：匿名 3 次/天、留资 5 次/天、VIP 不限；全站 150 次/天
- **意图澄清**——提问少于 80 字时弹选项补背景，够详细就直接推
- **方法库**（86）、**案例库**（76）、一图看懂可视化
- **账号系统**——账号+密码，**不做邮箱验证**（所以历史按用户 id 归属，不按邮箱，防串号）
- **个人中心** `/me`——资料自助编辑、历史一键分享、改密码
- **可分享报告** `/r/<id>`——公开只读页，带引回免费试 / 约 1:1 的钩子
- **MCP 端点**、**后台** `/admin/*`（看推演记录、留资、"翻车方法"）
- **对外 Skill 包 v2.0.0**——已构建通过，带脱敏 + 安全闸门 + 完整性闸门

---

## 四、拍过的板（别推翻，除非主人改口）

1. **产品公开化**：从"暗号墙"改成"免费试 + 留资转化"——原来暗号墙把获客端锁死了
2. **注册不要邮箱验证**：主人明确要求，降低摩擦；代价是不能用邮箱认人，所以按用户 id 归属
3. **MCP key 私发，不建后台**：现在用的人少，建后台是过度建设
4. **意图澄清用预设选项，不用大模型现生成**：模型要等 18 秒，还时不时返回空
5. **对外 Skill 走"厚包 + 回流钩子"**：方法卡随包分发，装上零依赖离线可用；
   靠输出末尾的署名和 CTA 引回。Skill 的职责是获客不是变现，门槛越低越好
6. **对外版是构建产物，私人版一个字不改**：两边手工维护必然漂移（已经吃过一次亏）
7. **深脑和 InnoLab 不合并**：深脑是记忆，InnoLab 是一只感官，走协议接线；
   深脑里有真人档案，**公开用户永远不能碰**

---

## 五、下一步（两条路，等主人挑）

**路一：把对外 Skill 包发布出去**
差最后一步——找个完全不懂 InnoLab 的 agent 装上包跑一道真题，确认第三方真能用。
盲测跑过一次但中途断了，没拿到报告。确认后就能发。

**路二：顾问现场仪表盘（价值密度最高）**
邱懿武在客户面前现场开的工具：会前预载客户背景、现场选方法、live 追问、免留资门、
输出带"为 X 客户定制"。需求卡已经写好在 `谱/要什么/01-顾问现场仪表盘.md`。
**动手前必须先问主人**：客户档案从哪进来（本地深脑库目录 / MCP / 手工粘贴）？

**悬着没拍板的**：方法库结构调整（合并 ST17+18、删 DC07/EV03、移 DC08/09/GN06）——
会改动方法 ID，牵连网站和 Skill 包，**不许随手动**。

---

## 六、踩过的坑（别再踩）

- macOS 上**别用 `sed -i` 做中文批量替换**，会挂；用 Python 脚本
- **别在后台命令里加 `&`**，会把进程弄死
- `deploy.py` 输出全缓冲，**长时间没输出 ≠ 挂了**
- **安全闸门写完必须做负向测试**——没触发过的安全网等于没有
- **方法卡里不许编造**研究、百分比、实验结论——之前清理过 5 处编造，这是方法库的立身之本

---

## 七、绝对不能碰的

1. **密钥一个字都不许进 git**——仓库是公开的
2. **不许动 `data/`**——线上真实用户数据（推演记录、留资），部署脚本已排除它
3. **改方法卡守 9 段结构**，原创方法不许瞎改
4. **对外分发必须走 `scripts/build-skill.py`**，不许手工打包（它负责脱敏和闸门）
5. **有安全欠账待还**——详见项目里 `谱/私密/安全欠账.md`（那个文件不在 git 里，
   在主人本机上）。简单说：服务器密码和大模型 token 需要轮换。

---

## 八、还想更细？

项目里这些文件是全的：

```
CLAUDE.md              项目卡（AI 每次开工先读这个）
谱/进度.md              现在到哪了、卡在哪、以后再说什么
谱/决定.md              每个决定 + 为什么
谱/要什么/              下一个功能具体什么样
谱/每天/                每天的工作记录
谱/私密/                安全欠账（不进 git，只在本机）
docs/                   飞轮架构、MCP 架构设计文档
references/METHOD_INDEX.md   86 个方法总索引
```
